extends ../../layouts/default.pug
include ../../mixins/box-head.pug

block main
  .container.my-3
    .row
      .col-12
        +box-head("Tour mới nhất")

    .row
      each item in toursNew
        .col-6.mb-3
          div(class="card")
            img(src=item.image)
            div(class="card-body")
              h5(class="card-title") #{item.title}
              b(class="mr-2 mb-2") #{item.price_special.toLocaleString()}đ
              del(class="mb-2") #{item.price.toLocaleString()}đ
              p(class="font-weight-bold") Giảm #{item.discount}%
              a(href=`/tours/detail/${item.slug}` class="btn btn-primary") Xem chi tiết

  .container.my-3
    .row
      .col-12
        +box-head("Danh mục mới nhất")
    .row
      each item in categoriesNew
        .col-6.mb-3
          div(class="card")
            img(src=item.image)
            div(class="card-body")
              h5(class="card-title") #{item.title}
              a(href=`/tours/${item.slug}` class="btn btn-primary") Xem chi tiết